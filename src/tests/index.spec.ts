describe("checkForExistingImage()", () => {
  it("should return True if an image with the provided filename exists", () => {});
  it("should return False if an image with the provided filename does not exist", () => {});
});

describe("processImage()", () => {
  it("should return existing thumbnail file path if it already exists", () => {});
  it(
    "should create and return the new processed thumbnail path if it does not exist "
  );
  it("should throw an error if the source/original image does not exist", () => {});
});
